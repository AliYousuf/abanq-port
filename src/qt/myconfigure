source ../../cross-mingw/xmingw32/bin/xmingw32.sh

export PREFIX=C:/facturalux
export QTDIR=$PWD
export PATH=$QTDIR/bin:$PATH
export QMAKESPEC=win32-g++-cross

# NOTAS 
######## 
# Compilar qmake, moc e uic nativamente en Linux
# Parámetros configure :
#./configure -prefix $PREFIX -platform linux-g++ -xplatform win32-g++-cross -buildkey "win32-g++-cross" -stl -disable-opengl